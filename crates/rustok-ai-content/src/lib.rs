#![cfg_attr(not(feature = "server"), allow(dead_code))]

/// Domain-owned registration entrypoint for content AI verticals.
pub fn register_content_ai_verticals() {
    // Placeholder for content_moderation registration.
}
