# Ревью админ-панелей RusTok: Leptos vs Next.js (референс)

## Обзор

В проекте две реализации админки:
- **Leptos Admin** (`apps/admin`) — основная, на Rust/WASM (Leptos CSR)
- **Next.js Admin** (`apps/next-admin`) — референсная реализация (Next.js 16 + shadcn/ui + Clerk)

Этот документ содержит результат сравнительного ревью и план по устранению проблем.

---

## 1. КРИТИЧЕСКИЕ ПРОБЛЕМЫ (исправить немедленно)

### 1.1 Дублирование кода — два параллельных варианта каждой страницы

**Проблема:** Для почти каждого файла существует `_new.rs` версия: `app.rs`/`app_new.rs`, `login.rs`/`login_new.rs`, `register.rs`/`register_new.rs`, `dashboard.rs`/`dashboard_new.rs`, `users.rs`/`users_new.rs`, `auth.rs`/`auth_new.rs`. При этом `*_new.rs` версии в основном неработоспособны — содержат мок-данные, ссылки на несуществующие API, или не компилируются.

**Что делать:**
- [ ] Определиться с одной версией для каждого файла
- [ ] Перенести рабочий функционал из старых версий в новые (или наоборот)
- [ ] Удалить дубликаты
- [ ] Обновить `mod.rs` для каждого модуля

### 1.2 Хранение JWT токенов в LocalStorage

**Проблема:** `providers/auth.rs` хранит JWT токен, данные пользователя и tenant slug в `LocalStorage`. Это уязвимо к XSS-атакам — любой скрипт на странице может прочитать токен.

**Что в Next.js:** Clerk управляет сессиями через HttpOnly cookies с SameSite=Strict.

**Что делать:**
- [ ] Перевести хранение токенов на HttpOnly cookies (требует изменений на сервере)
- [ ] Как минимум — добавить валидацию срока жизни токена
- [ ] Реализовать refresh-token механизм
- [ ] Добавить CSRF-защиту для форм

### 1.3 Верификационный токен отображается в UI

**Проблема:** В `register.rs:217-224` верификационный токен показывается пользователю в сообщении. Токен не должен быть видимым.

**Что делать:**
- [ ] Убрать отображение токена
- [ ] Показывать только сообщение "Проверьте почту"

### 1.4 Навигация в sidebar ведёт на несуществующие маршруты

**Проблема:** `sidebar.rs` содержит ссылки на `/posts`, `/pages`, `/media`, `/products`, `/orders`, `/customers`, `/settings` — ни один из этих маршрутов не существует в `app.rs`. Аналогично `dashboard_new.rs` ссылается на `/posts`, `/products`, `/settings`.

**Что в Next.js:** Навигация полностью синхронизирована с реальными маршрутами. Конфиг навигации (`nav-config.ts`) является единым источником правды.

**Что делать:**
- [ ] Синхронизировать sidebar с реальными маршрутами
- [ ] Создать единый конфиг навигации (аналог `nav-config.ts`)
- [ ] Убрать или пометить ссылки на планируемые, но нереализованные страницы

---

## 2. СЕРЬЁЗНЫЕ ПРОБЛЕМЫ (высокий приоритет)

### 2.1 Нет RBAC (Role-Based Access Control)

**Проблема:** `ProtectedRoute` проверяет только наличие токена (`auth.token.get().is_some()`). Нет проверки ролей, разрешений, плана подписки.

**Что в Next.js:** Декларативная система RBAC в `nav-config.ts` с полями `requireOrg`, `permission`, `role`, `plan`, `feature`. Хук `useFilteredNavItems` фильтрует навигацию по правам. Компонент `<Protect>` от Clerk блокирует доступ к страницам.

**Что делать:**
- [ ] Добавить роль пользователя в AuthContext
- [ ] Расширить `ProtectedRoute` — принимать параметры `required_role`, `required_permission`
- [ ] Фильтровать навигацию в sidebar по роли пользователя
- [ ] Добавить компонент `<RoleGuard>` для защиты отдельных элементов UI

### 2.2 Dashboard показывает захардкоженные данные

**Проблема:** `dashboard_new.rs` — все данные мок: "Total Users: 2,543", "Posts: 1,284", "Revenue: $45,231", проценты изменений тоже фейковые. `dashboard.rs` использует систему модулей, но данных нет.

**Что в Next.js:** Dashboard использует parallel routes с независимыми loading/error состояниями для каждого виджета. Данные приходят через mock API с реалистичной задержкой (1-3 секунды). Skeleton-компоненты показываются при загрузке.

**Что делать:**
- [ ] Реализовать GraphQL-запросы для статистики дашборда
- [ ] Добавить skeleton-загрузчики для каждого виджета
- [ ] Добавить графики (аналог Area/Bar/Pie из Next.js) — можно использовать Leptos-обёртки над JS chart-библиотеками или чистый SVG
- [ ] Обработать ошибки загрузки данных
- [ ] Добавить виджет "Последняя активность" с реальными данными

### 2.3 Нет CRUD-операций для пользователей

**Проблема:** Страница Users только просматривает список. Нет создания, редактирования, удаления пользователей. `user_details.rs` отображает данные, но не позволяет их менять. Кнопки в `users_new.rs` — заглушки.

**Что в Next.js:** Полный CRUD через Product management: таблица с фильтрами + создание/редактирование через форму + удаление через context-menu с подтверждением.

**Что делать:**
- [ ] Добавить форму создания пользователя
- [ ] Добавить редактирование пользователя на странице деталей
- [ ] Добавить удаление с модальным подтверждением (аналог `AlertModal` из Next.js)
- [ ] Добавить смену роли пользователя
- [ ] Добавить контекстное меню действий в таблице (Edit / Delete / Change Role)

### 2.4 Фильтрация пользователей на клиенте

**Проблема:** `users.rs:375-398` — фильтры применяются ПОСЛЕ загрузки всех данных. Каждый keystroke в поиске пересылает запрос без debounce.

**Что в Next.js:** Фильтрация через URL-параметры (nuqs), серверная пагинация, debounce 300ms в `use-data-table.ts`.

**Что делать:**
- [ ] Перенести фильтрацию на сервер (передавать фильтры в GraphQL-запрос)
- [ ] Добавить debounce на поисковый input (300-500ms)
- [ ] Сохранять фильтры в URL для bookmarkability
- [ ] Реализовать серверную пагинацию (offset/cursor)

### 2.5 Hardcoded API URL

**Проблема:** `api/mod.rs:7-8` содержит `http://localhost:3000/api/graphql` и `http://localhost:3000` прямо в коде. Нет конфигурации окружения.

**Что делать:**
- [ ] Загружать API URL из переменных окружения (или из `index.html` meta-тегов для WASM)
- [ ] Поддержать несколько окружений (dev/staging/prod)

---

## 3. ПРОБЛЕМЫ СРЕДНЕГО ПРИОРИТЕТА

### 3.1 Нет системы таблиц

**Проблема:** Таблица пользователей в `users.rs` сделана вручную через HTML `<table>`. Нет: сортировки по колонкам, выбора строк, настройки видимости колонок, контекстного меню.

**Что в Next.js:** TanStack React Table (headless) с DataTable, DataTableToolbar, DataTablePagination, DataTableFacetedFilter. Колонки описываются декларативно с метаданными для фильтров. Column pinning, row selection, сортировка, faceted-фильтры.

**Что делать:**
- [ ] Использовать `leptos-struct-table` (уже в зависимостях) для декларативных таблиц
- [ ] Добавить сортировку по колонкам
- [ ] Добавить выбор строк (checkbox)
- [ ] Добавить настройку видимости колонок
- [ ] Добавить faceted-фильтры (по роли, статусу)
- [ ] Добавить toolbar с поиском и фильтрами

### 3.2 Нет форм с валидацией

**Проблема:** Формы login/register используют простые сигналы без валидации. Нет клиентской проверки email-формата, силы пароля, обязательных полей.

**Что в Next.js:** React Hook Form + Zod — типобезопасные формы с декларативной схемой валидации. Generic-компоненты `FormInput`, `FormSelect`, `FormTextarea` с поддержкой обязательных полей, описаний, счётчика символов.

**Что делать:**
- [ ] Использовать `leptos-hook-form` + `leptos-zod` (уже в зависимостях) для форм
- [ ] Создать базовые компоненты форм: `FormInput`, `FormSelect`, `FormTextarea`
- [ ] Добавить Zod-схемы валидации для каждой формы
- [ ] Показывать ошибки валидации под полями
- [ ] Добавить индикатор обязательных полей (красная звёздочка)
- [ ] Добавить индикатор силы пароля при регистрации

### 3.3 Нет Kanban / управления задачами

**Что в Next.js:** Полноценная Kanban-доска с drag-and-drop (dnd-kit), Zustand-стор с persist, accessibility-аннотации для screen readers, колонки и карточки задач.

**Что делать:**
- [ ] Реализовать Kanban-страницу (если нужна в бизнес-логике)
- [ ] Использовать `leptos-dnd` или JS interop для drag-and-drop
- [ ] Zustand-подобный стор через `leptos-zustand` (уже в зависимостях)

### 3.4 Нет loading-состояний и skeleton-компонентов

**Проблема:** При загрузке данных нет визуальной обратной связи. Пользователь видит пустую страницу или прыжки контента.

**Что в Next.js:** Skeleton-компоненты для каждого типа контента: `DataTableSkeleton`, `BarGraphSkeleton`, `AreaGraphSkeleton`. Parallel routes с независимыми `loading.tsx`.

**Что делать:**
- [ ] Создать Skeleton-компоненты (карточки, таблицы, графики)
- [ ] Использовать `<Suspense>` / `<Transition>` из Leptos
- [ ] Добавить loading-индикаторы для всех async-операций
- [ ] Добавить error-fallback для каждого виджета

### 3.5 Заголовок страницы не меняется

**Проблема:** `header.rs:11-12` — `page_title` инициализируется как "Dashboard" и никогда не обновляется. На всех страницах отображается "Dashboard".

**Что в Next.js:** Динамические breadcrumbs, генерируемые по текущему маршруту (`useBreadcrumbs` хук).

**Что делать:**
- [ ] Реализовать динамический заголовок из контекста маршрута
- [ ] Добавить breadcrumbs-компонент
- [ ] Обновлять `<title>` документа

### 3.6 Sidebar не сворачивается

**Проблема:** Sidebar всегда развёрнут (264px). Нет мобильной версии (drawer/hamburger).

**Что в Next.js:** `<Sidebar collapsible='icon'>` — сворачивается до иконок. `<SidebarTrigger>` для мобильных устройств. Responsive дизайн.

**Что делать:**
- [ ] Добавить toggle для сворачивания sidebar до иконок
- [ ] Реализовать мобильный drawer (hamburger menu)
- [ ] Сохранять состояние sidebar в localStorage

### 3.7 Нет тем и dark mode

**Проблема:** Только одна светлая тема. Нет переключения light/dark.

**Что в Next.js:** 6+ тем (Claude, Mono, Neo-Brutalism, Notebook, Supabase, Vercel), переключатель light/dark mode, `ThemeProvider` + `ActiveThemeProvider`.

**Что делать:**
- [ ] Добавить dark mode через Tailwind `dark:` класс
- [ ] Реализовать переключатель тем
- [ ] Сохранять выбранную тему в localStorage

### 3.8 Нет Command Palette (Cmd+K)

**Что в Next.js:** kbar — быстрый поиск по навигации, действиям, переключение тем через клавиатурный шорткат.

**Что делать:**
- [ ] Реализовать Command Palette для быстрой навигации
- [ ] Добавить горячие клавиши для основных действий

---

## 4. ПРОБЛЕМЫ НИЗКОГО ПРИОРИТЕТА

### 4.1 Emoji вместо SVG-иконок

**Проблема:** Sidebar и dashboard используют emoji (👥, 📝, 📄) вместо иконок. Не масштабируются, не адаптируются под тему, не доступны для screen readers.

**Что в Next.js:** SVG-иконки из Tabler Icons, масштабируемые и тематизируемые.

**Что делать:**
- [ ] Заменить emoji на SVG-иконки (Lucide, Tabler, Heroicons)
- [ ] Добавить `aria-label` для accessibility

### 4.2 Нет accessibility

**Проблема:** Отсутствуют ARIA-атрибуты, семантический HTML, управление фокусом, поддержка клавиатурной навигации.

**Что в Next.js:** Radix UI (основа shadcn/ui) полностью accessible. Kanban board имеет live-region announcements для screen readers.

**Что делать:**
- [ ] Добавить `role`, `aria-label`, `aria-describedby` к интерактивным элементам
- [ ] Использовать семантические теги (`<nav>`, `<main>`, `<aside>`)
- [ ] Обеспечить keyboard navigation для таблиц и модальных окон

### 4.3 Нет мониторинга ошибок

**Что в Next.js:** Sentry интеграция через `instrumentation.ts` и `instrumentation-client.ts`.

**Что делать:**
- [ ] Интегрировать Sentry или аналог для отслеживания ошибок в WASM

### 4.4 Нет тестов

**Проблема:** В Leptos admin нет ни одного теста.

**Что делать:**
- [ ] Добавить unit-тесты для API-слоя
- [ ] Добавить component-тесты для ключевых компонентов
- [ ] Добавить E2E-тесты для auth-потока

### 4.5 GraphQL-запросы как строковые литералы

**Проблема:** Запросы в `users.rs` и `user_details.rs` — огромные строковые литералы с захардкоженными persisted query хэшами.

**Что делать:**
- [ ] Вынести запросы в отдельные `.graphql` файлы
- [ ] Использовать кодогенерацию для типов ответов
- [ ] Автоматически генерировать persisted query хэши

---

## 5. FEATURE GAP: что есть в Next.js, но отсутствует в Leptos

| Фича | Next.js | Leptos | Приоритет |
|-------|---------|--------|-----------|
| RBAC + permission-based навигация | Clerk + nav-config.ts | Нет | Высокий |
| Product management (CRUD + таблица) | Полный | Нет | Высокий |
| Skeleton loading states | Для каждого компонента | Нет | Высокий |
| Advanced data tables | TanStack + faceted filters | Простая HTML-таблица | Высокий |
| Form validation (Zod) | React Hook Form + Zod | Нет валидации | Высокий |
| Kanban board (drag-drop) | dnd-kit + Zustand persist | Нет | Средний |
| Multi-org / workspace switching | Clerk Organizations | Tenant slug в localStorage | Средний |
| Dark mode / themes | 6+ тем + light/dark | Нет | Средний |
| Command palette (Cmd+K) | kbar | Нет | Средний |
| Breadcrumbs | Автогенерация из маршрута | Нет | Средний |
| Charts (Area/Bar/Pie) | Recharts | Нет | Средний |
| Error boundaries per-widget | error.tsx at layout level | Нет | Средний |
| Parallel loading (independent) | Next.js parallel routes | Нет | Средний |
| Billing / subscription page | Clerk PricingTable | Нет | Низкий |
| File upload in forms | FormFileUpload | Нет | Низкий |
| Sentry error tracking | instrumentation.ts | Нет | Низкий |

---

## 6. РЕКОМЕНДУЕМЫЙ ПОРЯДОК РАБОТ

### Фаза 1: Расчистка (cleanup)
1. Решить, какую версию файлов оставить (старую или `_new`)
2. Удалить дубликаты
3. Исправить навигацию sidebar — убрать нерабочие ссылки
4. Вынести API URL в конфигурацию
5. Убрать отображение верификационного токена

### Фаза 2: Безопасность
1. Перевести токены с LocalStorage на HttpOnly cookies
2. Добавить CSRF-защиту
3. Реализовать валидацию срока жизни токена
4. Добавить refresh-token
5. Расширить ProtectedRoute: RBAC

### Фаза 3: Основной функционал
1. Реализовать серверную фильтрацию и пагинацию пользователей
2. Добавить CRUD для пользователей (создание, редактирование, удаление)
3. Подключить реальные данные к дашборду
4. Доделать профиль (avatar, timezone, locale)
5. Исправить страницу Security (CSS, функционал)

### Фаза 4: UX и компоненты
1. Создать систему таблиц (leptos-struct-table)
2. Создать систему форм с валидацией (leptos-hook-form + leptos-zod)
3. Добавить skeleton-загрузчики
4. Добавить breadcrumbs
5. Сделать sidebar сворачиваемым + мобильный drawer
6. Заменить emoji на SVG-иконки

### Фаза 5: Продвинутые фичи
1. Dark mode / тема
2. Графики на дашборде
3. Command palette
4. Kanban (если нужен)
5. Error tracking (Sentry)
6. Тесты
