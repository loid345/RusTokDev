# Alloy Manifest

**Версия:** 1.1  
**Суть:** Alloy — слой динамического поведения и интеллектуальной миграции в Rustok.

## 1. Что такое Alloy?
Alloy — это не «ещё одна CRM/ERP», а платформа для построения корпоративных систем под уникальные процессы компании.
В рамках Rustok она выполняет две роли:

- **Нервная система (Automation):** рефлексы и бизнес-автоматизация в реальном времени.
- **Желудок для Legacy (Migration):** «переваривание» грязных данных и перенос в чистую структуру.

> **Метафора:** Rust — это скелет и мышцы, Alloy — нервная система и «желудок» для legacy.

## 2. Зачем нужен Alloy?
Стандартные SaaS слишком жёсткие, заказная разработка — слишком дорогая и медленная. Alloy закрывает этот разрыв:

- **SaaS-платформам:** кастомизация логики клиентов без доступа к коду.
- **Enterprise-системам:** автоматизация workflow без постоянного участия разработчиков.
- **Low-code продуктам:** скриптовый слой поверх визуального конструктора.

Примеры использования:

- **CRM:** сделка зависла на 3 дня → скрипт ставит задачу и шлёт уведомление.
- **HR:** при найме создаются аккаунты, документы, онбординг-встречи.
- **Логистика:** изменение статуса доставки пересчитывает SLA и уведомляет клиента.
- **Финансы:** скоринг заявок из нескольких источников перед созданием инвойса.

## 3. Архитектура как организм

### 🦴 Скелет — Alloy Core (Rust)
Жёсткая структура данных, высоконагруженная логика, безопасность и скорость.

- Auth: пользователи, роли, сессии.
- Entities: сущности с динамическими полями (JSONB).
- Storage: PostgreSQL / Tarantool.

### 🧠 Нервная система — Alloy Scripting (Rhai)
Движок, реагирующий на события и применяющий правила.

- **Триггеры:** before/after/on_commit, cron, manual, API.
- **Песочница:** лимиты времени, памяти, операций; изоляция.
- **Bridge:** безопасный доступ скриптов к функциям ядра.
- **API управление:** GraphQL-слой в `apps/server` для CRUD и запуска скриптов.

### 🕸️ GraphQL API (Alloy Management)
Управление скриптами выполняется через единый GraphQL endpoint `apps/server`:

- **Query:** список/поиск скриптов, фильтрация по статусу, выборка по событиям.
- **Mutation:** создание, обновление, удаление, запуск вручную, управление статусом.
- **Безопасность:** операции доступны только административным ролям.

### 🧪 Желудок для Legacy — Migration Engine
Пакетная миграция и трансформация данных в больших объёмах.

- **Batch processing:** миллионы записей в одном прогоне.
- **Stream reader:** чтение из Postgres/MySQL/CSV/JSON.
- **Scripted transform:** один скрипт на трансформацию потока.

### 🧩 Органы — Модули
Готовые бизнес-блоки, подключаемые как плагины:

- CRM Module: сделки, контакты.
- HRM Module: сотрудники, отпуска.
- PM Module: проекты, задачи, спринты.

## 4. Два режима работы

| Режим | Назначение | Характеристики |
| --- | --- | --- |
| Runtime | Бизнес-автоматизация | События, хуки, real-time, 1 entity за раз |
| Migration | Перенос данных | Batch, миллионы записей, один скрипт трансформации |

Обе ветки используют общий Rhai Engine, Bridge и EntityProxy, различается только Runner.

## 5. Поток выполнения (Alloy Flow)
Любое runtime-действие проходит через событийную модель:

1. **before_*** — валидация и изменения данных (может вызвать `abort`).
2. **Core** — основная операция Rust (ACID).
3. **after_*** — связанные изменения в транзакции (ошибка → откат).
4. **on_commit** — внешние эффекты (webhook, email, Slack).

## 6. Ключевые принципы

1. **Safety First** — скрипт не должен «ронять» сервер (таймауты, лимиты, auto-disable по правилу 3 ошибок).
2. **Hybrid Intelligence** — системная логика в Git, пользовательская логика в БД через UI.
3. **No Magic** — скрипт видит только разрешённое (permission intersection).
4. **Performance** — Rust обрабатывает тысячи запросов, скрипты запускаются только там, где нужна кастомизация.
5. **Graceful Degradation** — сбой скрипта не ломает основную операцию.

## 7. Итог
Alloy — это сплав производительности Rust и гибкости скриптов, который позволяет бизнесу строить свои процессы и аккуратно поглощать legacy-данные, а не подстраиваться под чужие ограничения.

This is an alpha version and requires clarification. Be careful, there may be errors in the text. So that no one thinks that this is an immutable rule.
