# RusToK Quickstart Guide

Быстрый старт для локальной разработки с двумя админками (Next.js + Leptos) и двумя витринами.

> 🚨 **ПЕРЕД НАЧАЛОМ РАБОТЫ ОБЯЗАТЕЛЬНО ПРОЧИТАЙТЕ:**
>
> 1. [`docs/UI/CRITICAL_WARNINGS.md`](docs/UI/CRITICAL_WARNINGS.md) — критичные предупреждения
> 2. [`docs/UI/MASTER_IMPLEMENTATION_PLAN.md`](docs/UI/MASTER_IMPLEMENTATION_PLAN.md) — 🆕 **главный план реализации**
> 3. [`docs/UI/CUSTOM_LIBRARIES_STATUS.md`](docs/UI/CUSTOM_LIBRARIES_STATUS.md) — 🆕 **статус самописных библиотек**
>
> **Ключевые моменты:**
> - ⚠️ НЕ УДАЛЯЙТЕ библиотеки из `crates/*` — мы их починим
> - ⚠️ Используйте НАШИ самописные библиотеки из `crates/leptos-*`
> - ✅ **Используйте `MASTER_IMPLEMENTATION_PLAN.md`**, старые планы deprecated

## 🚀 Запуск одной командой

```bash
# 1. Клонировать репозиторий (если еще не сделано)
git clone <repo-url>
cd rustok

# 2. Запустить весь стек
./scripts/dev-start.sh
```

**Вот и всё!** 🎉

Скрипт автоматически:
- Создаст `.env.dev` из примера (если не существует)
- Запустит PostgreSQL базу данных
- Запустит RusToK server (backend API)
- Запустит обе админки (Next.js на :3000, Leptos на :3001)
- Запустит обе витрины (Next.js на :3100, Leptos на :3101)
- Создаст seed данные с admin пользователем

## 📱 Доступ к сервисам

После запуска откройте в браузере:

### Backend
- **API Server**: http://localhost:5150
- **GraphQL Playground**: http://localhost:5150/api/graphql
- **Health Check**: http://localhost:5150/api/health

### Админки
- **Next.js Admin**: http://localhost:3000
- **Leptos Admin**: http://localhost:3001

### Витрины
- **Next.js Storefront**: http://localhost:3100
- **Leptos Storefront**: http://localhost:3101

### База данных
- **PostgreSQL**: localhost:5432
- **Database**: rustok_dev
- **User**: rustok
- **Password**: rustok

## 🔑 Тестовые данные

Для входа в админку используйте:

```
Email:    admin@local
Password: admin12345
```

> ⚠️ **Важно**: Эти данные только для локальной разработки! Не используйте в production.

## 🛠 Полезные команды

```bash
# Остановить все сервисы
./scripts/dev-start.sh stop

# Перезапустить все сервисы
./scripts/dev-start.sh restart

# Посмотреть логи всех сервисов
./scripts/dev-start.sh logs

# Посмотреть логи конкретного сервиса
./scripts/dev-start.sh logs server
./scripts/dev-start.sh logs admin-next
./scripts/dev-start.sh logs admin-leptos

# Посмотреть статус сервисов
./scripts/dev-start.sh status

# Запустить только админки (без storefront)
./scripts/dev-start.sh start admin

# Запустить только server и database
./scripts/dev-start.sh start server

# Помощь
./scripts/dev-start.sh --help
```

## 📦 Что установлено?

Проект использует Docker Compose, поэтому вам нужны только:
- Docker
- Docker Compose

Все остальное (Rust, Node.js, зависимости) установлено внутри контейнеров.

## 🔧 Ручная установка (без Docker)

Если хотите запускать сервисы локально без Docker:

### Требования
- Rust 1.82+ (через rustup)
- Node.js 20+ (рекомендуем через nvm)
- Bun (для Next.js админки)
- PostgreSQL 16+
- Trunk (для Leptos: `cargo install trunk`)

### Запуск

```bash
# 1. Настроить базу данных
createdb rustok_dev
# Применить миграции (из apps/server)

# 2. Запустить backend
cd apps/server
cargo run

# 3. Запустить Next.js админку (в другом терминале)
cd apps/next-admin
bun install
bun run dev

# 4. Запустить Leptos админку (в другом терминале)
cd apps/admin
trunk serve --port 3001

# Аналогично для storefront
```

## 🐛 Устранение неполадок

### Порты заняты

Если порты 3000, 3001, 3100, 3101, 5150 уже заняты:
1. Остановите другие сервисы на этих портах
2. Или измените порты в `.env.dev`

### Ошибка подключения к базе данных

```bash
# Проверить, что контейнер PostgreSQL запущен
docker compose ps db

# Посмотреть логи базы данных
./scripts/dev-start.sh logs db
```

### Server не стартует

```bash
# Посмотреть логи сервера
./scripts/dev-start.sh logs server

# Проверить healthcheck
curl http://localhost:5150/api/health
```

### Админка не загружается

1. Проверьте, что server работает: http://localhost:5150/api/health
2. Проверьте логи: `./scripts/dev-start.sh logs admin-next`
3. Проверьте `.env.dev` — правильный ли `NEXT_PUBLIC_API_URL`

## 📚 Дополнительная документация

- [Детальный план реализации](docs/UI/ADMIN_IMPLEMENTATION_PLAN.md)
- [Миграция шаблона админки](docs/UI/admin-template-migration.md)
- [Подключение админки к серверу](docs/UI/admin-server-connection-quickstart.md)
- [Паритет библиотек](docs/UI/admin-libraries-parity.md)
- [Основной README](README.md)

## 🎯 Следующие шаги

1. ✅ Убедитесь, что все сервисы запущены
2. ✅ Откройте http://localhost:3000 и залогиньтесь
3. ✅ Откройте http://localhost:3001 (Leptos версия)
4. 📖 Изучите [план реализации](docs/UI/ADMIN_IMPLEMENTATION_PLAN.md)
5. 💻 Начните разработку следуя фазам из плана

---

**Удачной разработки!** 🚀

Если возникли вопросы или проблемы, создайте issue в репозитории.
