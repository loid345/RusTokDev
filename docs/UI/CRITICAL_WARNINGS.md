# 🚨 КРИТИЧЕСКИЕ ПРЕДУПРЕЖДЕНИЯ ДЛЯ РАЗРАБОТЧИКОВ

**Дата:** 2026-02-13  
**Статус:** Обязательно к прочтению перед началом работы

---

## ⚠️ ПРЕДУПРЕЖДЕНИЕ #1: НЕ УДАЛЯЙТЕ БИБЛИОТЕКИ

### 🛑 СТРОГО ЗАПРЕЩЕНО

**НИКОГДА не удаляйте библиотеки из проекта, даже если они кажутся нерабочими или неиспользуемыми.**

### Почему это важно?

У нас есть **самописные библиотеки** в `crates/*`, которые:
- Могут быть в процессе разработки
- Могут иметь временные проблемы с интеграцией
- Могут быть не полностью задокументированы
- Но они являются **частью архитектуры** и будут починены

### Что делать, если библиотека не работает?

1. ✅ **Зафиксировать проблему:**
   - Создать issue с тегом `library-issue`
   - Описать: что не работает, какая версия, minimal repro
   - Прикрепить логи и скриншоты

2. ✅ **Проверить документацию:**
   - `docs/UI/admin-libraries-parity.md` — список библиотек и их статус
   - README библиотеки в `crates/<lib-name>/`
   - Официальная документация (если есть)

3. ✅ **Попробовать исправить интеграцию:**
   - Проверить совместимость версий
   - Проверить правильность импортов
   - Проверить конфигурацию

4. ✅ **Запросить помощь:**
   - Создать вопрос в issue с тегом `help-wanted`
   - Описать, что уже попробовали
   - Указать deadline (если критично)

5. ⚠️ **Временный workaround (крайняя мера):**
   - Если библиотека блокирует разработку
   - Создать **временный** локальный компонент в `apps/*/src/components/temp/`
   - Обязательно добавить комментарий: `// TODO: заменить на crates/<lib-name> после фикса`
   - Создать issue с тегом `technical-debt`

### ❌ ЗАПРЕЩЕННЫЕ действия

- ❌ Удалить библиотеку из `Cargo.toml` или `package.json`
- ❌ Закомментировать импорты библиотеки
- ❌ Переписать функционал на "временный" самопис без согласования
- ❌ Заменить библиотеку на альтернативную без RFC/approval

### Процесс замены библиотеки (если действительно нужно)

1. Создать RFC (Request for Comments) в issue:
   - Описать проблему с текущей библиотекой
   - Предложить альтернативу с обоснованием
   - Описать риски и план миграции
   - Получить approval от team lead или архитектора

2. Только после approval:
   - Создать migration plan
   - Обновить документацию (`admin-libraries-parity.md`)
   - Выполнить миграцию постепенно
   - Обновить все зависимые компоненты

---

## ⚠️ ПРЕДУПРЕЖДЕНИЕ #2: ШАБЛОН УЖЕ ПЕРЕНЕСЕН

### 📁 Актуальное расположение

**Шаблон Next.js Dashboard находится в:** `apps/next-admin`

~~`vendor/ui/next-shadcn-dashboard-starter`~~ — НЕ ИСПОЛЬЗУЕТСЯ (устаревшая информация)

### Что это означает?

- ✅ Шаблон готов к адаптации под наш backend API
- ✅ Все компоненты уже доступны в `apps/next-admin/src/components/`
- ✅ Все страницы уже в `apps/next-admin/src/app/`
- ⚠️ Но они используют **моковую логику** (faker.js, локальные хуки)

### Ваша задача

1. **Адаптировать Next.js Admin (`apps/next-admin`):**
   - Заменить моковые данные на GraphQL запросы
   - Подключить `leptos-graphql` или GraphQL client
   - Удалить Clerk auth → использовать наш `/api/auth/*`
   - Использовать наши самописные библиотеки (см. ниже)

2. **Параллельно реализовать в Leptos Admin (`apps/admin`):**
   - Взять UI/UX из Next.js admin как референс
   - Реализовать на Leptos с нашими библиотеками
   - Обеспечить функциональный паритет

---

## ⚠️ ПРЕДУПРЕЖДЕНИЕ #3: ИСПОЛЬЗУЙТЕ НАШИ БИБЛИОТЕКИ

### 🦀 Самописные библиотеки в `crates/*`

У нас есть **собственные реализации** для обеих админок:

#### Для Leptos Admin (`apps/admin`)

| Библиотека | Назначение | Статус | Замена для |
|------------|------------|--------|------------|
| `leptos-auth` | Авторизация и аутентификация | ✅ Рабочая | NextAuth, Clerk |
| `leptos-graphql` | GraphQL client transport | ✅ Рабочая | Apollo, urql |
| `leptos-hook-form` | Управление формами | ⚠️ В разработке | react-hook-form |
| `leptos-zod` | Валидация схем | ⚠️ В разработке | zod (адаптер) |
| `leptos-table` | Таблицы и data grids | ⚠️ В разработке | @tanstack/react-table |
| `leptos-struct-table` | Структурированные таблицы | ⚠️ В разработке | @tanstack/react-table |
| `leptos-shadcn-pagination` | Пагинация | ✅ Рабочая | shadcn pagination |
| `leptos-zustand` | State management | ⚠️ В разработке | zustand |
| `leptos-use` | Browser/reactive utils | ✅ Рабочая | react-use |
| `leptos_i18n` | Интернационализация | ✅ Рабочая | next-i18next |
| `leptos-next-metadata` | Metadata/SEO | ⚠️ В разработке | next/head |
| `tailwind-rs` | Tailwind для Leptos | ✅ Рабочая | tailwindcss |

#### Для Next.js Admin (`apps/next-admin`)

| Библиотека | Назначение | Что использовать |
|------------|------------|------------------|
| Auth | Авторизация | **НАШ** `/api/auth/*` API, НЕ Clerk |
| GraphQL | API запросы | GraphQL client к `/api/graphql` |
| Forms | Формы | react-hook-form + zod (стандартные, НО валидация через наш backend) |
| Tables | Таблицы | @tanstack/react-table (стандартная) |
| State | Состояние | zustand (стандартная) |

### Статусы библиотек

- ✅ **Рабочая** — используйте как есть
- ⚠️ **В разработке** — может иметь баги, но НЕ УДАЛЯЙТЕ, мы починим
- ❌ **Gap** — требуется создание (см. Gap Log)

### Если библиотека "в разработке" не работает

**НЕ ПАНИКУЙТЕ.** Это нормально.

1. Создайте issue с описанием проблемы
2. Используйте временный workaround (см. Предупреждение #1)
3. Мы починим библиотеку и вы сможете заменить workaround

---

## ⚠️ ПРЕДУПРЕЖДЕНИЕ #4: MODULE-FIRST APPROACH

### Где размещать код

#### ✅ ПРАВИЛЬНО: В модулях (`crates/*`)

**Вся бизнес-логика, API клиенты, shared компоненты должны быть в `crates/*`:**

- `crates/rustok-core` — auth, tenant, RBAC, инфраструктура
- `crates/rustok-content` — контент (CMS)
- `crates/rustok-blog` — блог
- `crates/rustok-commerce` — e-commerce
- `crates/rustok-forum` — форум
- `crates/rustok-index` — read models, индексы
- `crates/leptos-*` — shared UI библиотеки для Leptos
- `crates/leptos-shadcn-ui` — UI primitives

#### ⚠️ ДОПУСТИМО: В `apps/*` (только UI композиция)

**В `apps/next-admin` и `apps/admin` должны быть ТОЛЬКО:**

- Layout компоненты (композиция)
- Page компоненты (композиция)
- Routing конфигурация
- Специфичные для приложения настройки

#### ❌ НЕПРАВИЛЬНО: Временный код в `apps/*`

**НЕ создавайте в `apps/*`:**

- API клиенты (должны быть в `crates/leptos-graphql` или аналогах)
- Бизнес-логику (должна быть в `crates/rustok-*`)
- Переиспользуемые компоненты (должны быть в `crates/leptos-*`)
- Утилиты и хелперы (должны быть в `crates/rustok-core`)

### Правило module-first delivery

1. **Сначала** определить целевой модуль (backend crate и/или shared frontend crate)
2. **Реализовать** API-контракт внутри модуля
3. **Подключить** модуль в Next и Leptos экранах (параллельно)
4. **Зафиксировать** в changelog/плане, в какой модуль легла функциональность

---

## ⚠️ ПРЕДУПРЕЖДЕНИЕ #5: ПАРАЛЛЕЛЬНАЯ РАЗРАБОТКА ОБЯЗАТЕЛЬНА

### Next.js + Leptos одновременно

**Функциональный паритет обязателен.**

Если вы реализовали фичу в `apps/next-admin`, вы ОБЯЗАНЫ:

1. Создать аналогичную фичу в `apps/admin` (Leptos)
2. Или создать parity-task в issue с тегом `parity-gap`
3. Deadline для устранения parity-gap: не более одного спринта

### Что означает "паритет"?

- ✅ Одинаковые страницы в обеих админках
- ✅ Одинаковые функции (создать, редактировать, удалить)
- ✅ Одинаковые RBAC проверки
- ✅ Одинаковые навигация и layout
- ⚠️ Допустимы небольшие различия в UX (из-за разных фреймворков)

### Трекинг паритета

Обновляйте статус в:
- `docs/UI/PROGRESS_SUMMARY.md` — общий прогресс
- `docs/UI/admin-libraries-parity.md` — паритет библиотек
- Issue с тегом `parity-gap` — конкретные задачи

---

## 📋 Чек-лист перед началом работы

Прочитайте и отметьте:

- [ ] Я прочитал это предупреждение полностью
- [ ] Я понимаю, что НЕ МОГУ удалять библиотеки
- [ ] Я знаю, что шаблон находится в `apps/next-admin`
- [ ] Я буду использовать наши самописные библиотеки из `crates/*`
- [ ] Я знаю, что код должен идти в модули, а не в `apps/*`
- [ ] Я буду обеспечивать паритет Next.js ↔ Leptos
- [ ] Я буду создавать issue при обнаружении проблем
- [ ] Я прочитал `docs/UI/admin-libraries-parity.md`
- [ ] Я прочитал `docs/UI/ADMIN_IMPLEMENTATION_PLAN.md`
- [ ] Я готов следовать плану реализации

---

## 🔗 Обязательные документы к прочтению

**Перед началом работы прочитайте:**

1. `docs/UI/ADMIN_IMPLEMENTATION_PLAN.md` — детальный план реализации
2. `docs/UI/admin-libraries-parity.md` — паритет библиотек и gap log
3. `docs/UI/admin-template-migration.md` — как адаптировать шаблон
4. `docs/UI/agent-execution-guide.md` — инструкция для агента
5. `QUICKSTART.md` — быстрый старт

**При возникновении проблем:**

6. `docs/UI/admin-server-connection-quickstart.md` — подключение к серверу
7. `docs/UI/PROGRESS_SUMMARY.md` — текущий прогресс и backlog

---

## 💬 Куда обращаться за помощью

### При проблемах с библиотекой

1. Создайте issue с тегом `library-issue`
2. Укажите название библиотеки, версию, проблему
3. Прикрепите логи и minimal repro
4. Команда поможет или починит библиотеку

### При вопросах по архитектуре

1. Создайте discussion с тегом `architecture`
2. Опишите ситуацию и возможные варианты
3. Дождитесь feedback от team lead

### При urgent блокерах

1. Создайте issue с тегом `blocker`
2. Укажите deadline и влияние на проект
3. Предложите временный workaround (если есть)

---

## 🎯 Главное правило

> **МЫ ВСЕГДА ПОЧИНИМ БИБЛИОТЕКИ. НЕ УДАЛЯЙТЕ ИХ.**

Библиотеки в `crates/*` — это наша инвестиция в долгосрочное развитие проекта.

Даже если библиотека временно не работает, это не повод её удалять.

**Мы починим её** и все приложения автоматически получат исправление.

---

**Версия:** 1.0  
**Дата:** 2026-02-13  
**Автор:** CTO Agent

**⚠️ Это обязательный документ для всех разработчиков.**
