# Алгоритм параллельной разработки Next.js ↔ Leptos с дописыванием библиотек

**Дата:** 2026-02-13  
**Версия:** 1.0  
**Статус:** Обязательно к прочтению перед началом работы

---

## 📋 Контекст

Мы разрабатываем **две админки параллельно**:
- `apps/next-admin` (Next.js + React) — быстрый прототип на готовом starter
- `apps/admin` (Leptos + Rust WASM) — production-ready админка

**Цель:** обеспечить функциональный паритет между обеими админками при минимальном дублировании кода.

**Решение:** все переиспользуемые компоненты и логику выносим в `crates/*`, а в `apps/*` остается только UI композиция.

---

## 🎯 Основной принцип: Module-First

```
┌─────────────────────────────────────────────────────────────┐
│  apps/next-admin (React)         apps/admin (Leptos)        │
│  ┌─────────────────┐              ┌─────────────────┐       │
│  │ UI Composition  │              │ UI Composition  │       │
│  │ (JSX/TSX)       │              │ (Leptos macros) │       │
│  └────────┬────────┘              └────────┬────────┘       │
│           │                                │                 │
│           └────────────┬───────────────────┘                 │
│                        ▼                                     │
│         ┌──────────────────────────────────┐                │
│         │   Shared Libraries (crates/*)    │                │
│         ├──────────────────────────────────┤                │
│         │ leptos-auth                      │ ◄─── Auth      │
│         │ leptos-graphql                   │ ◄─── API       │
│         │ leptos-hook-form + leptos-zod    │ ◄─── Forms     │
│         │ leptos-table + leptos-pagination │ ◄─── Tables    │
│         │ leptos-zustand                   │ ◄─── State     │
│         │ leptos-use                       │ ◄─── Utils     │
│         │ leptos-shadcn-ui                 │ ◄─── UI        │
│         │ ... (и другие)                   │                │
│         └──────────────────────────────────┘                │
│                        ▲                                     │
│                        │                                     │
│         ┌──────────────┴──────────────────┐                 │
│         │   Backend API (apps/server)     │                 │
│         │   /api/graphql, /api/auth/*     │                 │
│         └─────────────────────────────────┘                 │
└─────────────────────────────────────────────────────────────┘
```

**Правило:** Код в `crates/*`, НЕ в `apps/*`

---

## 🔄 Алгоритм параллельной разработки

### Шаг 1: Анализ задачи

**Вход:** задача из [ADMIN_IMPLEMENTATION_PLAN.md](./ADMIN_IMPLEMENTATION_PLAN.md)

**Действия:**
1. ✅ Прочитать описание задачи в плане (какая фаза, какой функционал)
2. ✅ Проверить [admin-libraries-parity.md](./admin-libraries-parity.md) — есть ли библиотека?
3. ✅ Определить, куда идет код:
   - `crates/*` — если переиспользуемая логика/компонент
   - `apps/*` — только если UI композиция страницы

**Выход:** понимание, где реализовывать функционал

---

### Шаг 2: Выбор библиотеки

**Сценарий A: Библиотека существует и работает ✅**

Пример: `leptos-auth`, `leptos-graphql`, `leptos-use`

**Действия:**
1. ✅ Использовать библиотеку как есть
2. ✅ Подключить в `apps/admin/Cargo.toml`
3. ✅ Реализовать функционал в обеих админках:
   - `apps/next-admin` — использовать React-аналог или стандартную библиотеку
   - `apps/admin` — использовать нашу библиотеку из `crates/*`

**Пример:**
```rust
// apps/admin/src/pages/auth/sign_in.rs
use leptos_auth::{use_auth, SignInForm};  // ✅ Используем готовую библиотеку

#[component]
pub fn SignInPage() -> impl IntoView {
    let auth = use_auth();
    
    view! {
        <SignInForm 
            on_submit=move |credentials| {
                auth.sign_in(credentials)
            }
        />
    }
}
```

```tsx
// apps/next-admin/src/app/auth/sign-in/page.tsx
import { useAuth } from '@/lib/auth/rustok-auth'  // ✅ Адаптер к нашему backend

export default function SignInPage() {
    const { signIn } = useAuth()
    
    return (
        <SignInForm 
            onSubmit={(credentials) => signIn(credentials)}
        />
    )
}
```

---

**Сценарий B: Библиотека в разработке ⚠️**

Пример: `leptos-hook-form`, `leptos-zod`, `leptos-table`

**Действия:**
1. ⚠️ **НЕ УДАЛЯТЬ библиотеку!**
2. ✅ Попробовать использовать как есть
3. ❌ Если не работает:
   - Создать issue с тегом `library-issue`
   - Описать проблему: версия, логи, minimal repro
   - Создать **временный workaround** в `apps/*/src/components/temp/`
   - Добавить комментарий: `// TODO: заменить на crates/<lib-name> после фикса`
4. ✅ Продолжить разработку с workaround
5. ✅ После фикса библиотеки — заменить workaround

**Пример workaround:**
```rust
// apps/admin/src/components/temp/simple_form.rs
// TODO: заменить на leptos-hook-form после фикса issue #123

use leptos::*;

#[component]
pub fn SimpleForm(
    on_submit: impl Fn(FormData) + 'static
) -> impl IntoView {
    let (form_data, set_form_data) = create_signal(FormData::default());
    
    // Временная реализация без leptos-hook-form
    view! {
        <form on:submit=move |ev| {
            ev.prevent_default();
            on_submit(form_data.get())
        }>
            // ... поля формы ...
        </form>
    }
}
```

**Issue пример:**
```markdown
Title: leptos-hook-form: register() не работает с dynamic fields

**Library:** crates/leptos-hook-form v0.1.0
**Problem:** При вызове register() с динамическими полями получаем panic
**Expected:** Поля должны регистрироваться без ошибок
**Logs:**
```
thread 'main' panicked at 'index out of bounds: the len is 0 but the index is 1'
```

**Minimal repro:**
```rust
let form = use_form();
form.register("dynamic_field"); // <-- panic here
```

**Workaround:** Используем временный `SimpleForm` в `apps/admin/src/components/temp/`
**Deadline:** Sprint 2
```

---

**Сценарий C: Библиотеки нет ❌ (Gap)**

Пример: `leptos-command-palette`, `leptos-dropzone`, `leptos-dnd`

**Действия:**
1. ✅ Проверить [admin-libraries-parity.md](./admin-libraries-parity.md) — может быть кандидат?
2. ✅ Проверить [rust-ui-component-catalog.md](./rust-ui-component-catalog.md) — может быть готовый компонент из Rust/UI?
3. ✅ Выбрать один из путей:

**Путь A: Адаптировать из Rust/UI**
```bash
# 1. Найти компонент в Rust/UI
# https://www.rust-ui.com/docs/components/<component-name>

# 2. Скопировать код компонента в наш crate
mkdir -p crates/leptos-<component-name>/src
# ... скопировать код ...

# 3. Адаптировать под наши нужды
# - Убрать зависимости от Rust/UI инфраструктуры
# - Добавить в Cargo.toml
# - Экспортировать публичный API

# 4. Обновить документацию
# - admin-libraries-parity.md: добавить библиотеку со статусом ✅
# - rust-ui-component-catalog.md: пометить decision=adopt
```

**Путь B: Использовать существующую crates.io библиотеку**
```bash
# 1. Поиск на crates.io
# Пример: leptos_dnd для drag-and-drop

# 2. Проверка критериев
# - Совместимость с нашей версией leptos
# - SSR/CSR поддержка (если нужно)
# - Активная разработка (недавние коммиты)
# - Разумная лицензия (MIT/Apache)

# 3. Создать wrapper в нашем crate
mkdir -p crates/leptos-<feature>/src

# 4. Обернуть в единый API
# - Скрыть сложность внешней библиотеки
# - Добавить наши типы и обработку ошибок
# - Экспортировать простой API
```

**Путь C: Создать с нуля (крайняя мера)**
```bash
# 1. Создать новый crate
cargo new --lib crates/leptos-<feature>

# 2. Реализовать минимальный MVP
# - Только критичный функционал
# - Простой API
# - Хорошая документация

# 3. Добавить тесты и примеры

# 4. Зарегистрировать в workspace
# Cargo.toml (root):
# [workspace]
# members = [..., "crates/leptos-<feature>"]
```

**Обязательно:**
- ✅ Создать `crates/<lib-name>/README.md` с API документацией
- ✅ Обновить [admin-libraries-parity.md](./admin-libraries-parity.md)
- ✅ Обновить [rust-ui-component-catalog.md](./rust-ui-component-catalog.md) если source=rust-ui
- ✅ Создать issue с тегом `new-library` для трекинга

---

### Шаг 3: Параллельная реализация

**Принцип:** Реализуем функционал в ОБЕИХ админках одновременно (или создаем parity-task)

**Workflow:**

```
┌─────────────────────────────────────────────────────────┐
│ 1. Реализация в Next.js Admin (apps/next-admin)        │
│    - Быстрый прототип на готовом starter               │
│    - Используем React-библиотеки или наши адаптеры     │
│    - UI/UX референс для Leptos                          │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│ 2. Реализация в Leptos Admin (apps/admin)              │
│    - Используем наши библиотеки из crates/*             │
│    - Копируем UI/UX из Next.js (Tailwind классы)        │
│    - Добавляем Rust типы и safety                       │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│ 3. Проверка паритета                                    │
│    - Функционал работает в обеих админках? ✅           │
│    - UI выглядит одинаково? ✅                          │
│    - API запросы корректные? ✅                         │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│ 4. Если паритет НЕ достигнут                            │
│    - Создать issue с тегом parity-gap                   │
│    - Deadline: не более одного спринта                  │
│    - Зафиксировать в PROGRESS_SUMMARY.md                │
└─────────────────────────────────────────────────────────┘
```

**Пример параллельной реализации:**

**Next.js Admin:**
```tsx
// apps/next-admin/src/app/dashboard/page.tsx
import { DashboardStats } from '@/components/dashboard/stats'
import { RecentActivity } from '@/components/dashboard/activity'
import { useGraphQL } from '@/lib/graphql/client'

export default function DashboardPage() {
    const { data, loading } = useGraphQL(`
        query Dashboard {
            stats { users, revenue, orders }
            recentActivity { ... }
        }
    `)
    
    if (loading) return <Skeleton />
    
    return (
        <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
            <DashboardStats stats={data.stats} />
            <RecentActivity items={data.recentActivity} />
        </div>
    )
}
```

**Leptos Admin (с нашими библиотеками):**
```rust
// apps/admin/src/pages/dashboard.rs
use leptos::*;
use leptos_graphql::use_query;  // ✅ Наша библиотека
use crate::components::dashboard::{DashboardStats, RecentActivity};

#[component]
pub fn DashboardPage() -> impl IntoView {
    let dashboard_query = use_query(
        "Dashboard",
        r#"query Dashboard {
            stats { users, revenue, orders }
            recentActivity { ... }
        }"#
    );
    
    view! {
        <Suspense fallback=|| view! { <Skeleton /> }>
            {move || {
                dashboard_query.data().map(|data| view! {
                    <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
                        <DashboardStats stats=data.stats />
                        <RecentActivity items=data.recent_activity />
                    </div>
                })
            }}
        </Suspense>
    }
}
```

**Компоненты (в crates/ если переиспользуются):**

Если `DashboardStats` нужен ТОЛЬКО в админке:
- ✅ `apps/admin/src/components/dashboard/stats.rs`
- ✅ `apps/next-admin/src/components/dashboard/stats.tsx`

Если `DashboardStats` нужен в админке И storefront:
- ✅ `crates/leptos-dashboard/src/stats.rs` (shared)
- ✅ Обе админки используют shared компонент

---

### Шаг 4: Обновление документации

**Обязательно обновить:**

1. **[PROGRESS_SUMMARY.md](./PROGRESS_SUMMARY.md)**
   - Отметить выполненную задачу
   - Обновить прогресс фазы
   - Добавить в backlog parity-gap (если есть)

2. **[admin-libraries-parity.md](./admin-libraries-parity.md)**
   - Если добавили новую библиотеку — добавить в таблицу
   - Обновить статус: `✅ Закрыто` / `⚠️ Частично` / `❌ Gap`
   - Обновить явную замену и действие

3. **[rust-ui-component-catalog.md](./rust-ui-component-catalog.md)** (если использовали Rust/UI)
   - Пометить компонент как `adopt` / `pilot` / `reject`
   - Указать `target crate`
   - Указать `owner`

4. **[ADMIN_IMPLEMENTATION_PLAN.md](./ADMIN_IMPLEMENTATION_PLAN.md)** (если изменился порядок)
   - Обновить статус задачи: `[ ]` → `[x]`
   - Добавить примечания (если были сложности)

---

## 🚨 Критические правила

### Правило 1: НЕ УДАЛЯТЬ БИБЛИОТЕКИ

**НИКОГДА не удаляйте библиотеки из `crates/*` без RFC и approval.**

**Если библиотека не работает:**
1. ✅ Создать issue с тегом `library-issue`
2. ✅ Использовать временный workaround
3. ✅ Дождаться фикса и заменить workaround

**НЕ делать:**
- ❌ Удалять библиотеку из `Cargo.toml`
- ❌ Комментировать импорты
- ❌ Переписывать на самопис без согласования

**Процесс замены (если действительно нужно):**
1. Создать RFC в issue
2. Обосновать: почему текущая не подходит
3. Предложить альтернативу с trade-offs
4. Описать план миграции
5. Получить approval
6. Только после approval — заменить

---

### Правило 2: Module-First подход

**Код размещается:**

✅ **В `crates/*` если:**
- Переиспользуется в 2+ приложениях
- Содержит бизнес-логику
- Это UI компонент для shared библиотеки
- Это API клиент или adapter

❌ **НЕ в `apps/*` если:**
- Это не UI композиция страницы
- Это переиспользуемый компонент
- Это бизнес-логика
- Это API клиент

✅ **В `apps/*` только если:**
- UI композиция страницы (page components)
- Routing конфигурация
- Layout композиция
- Специфичные для app настройки

---

### Правило 3: Параллельная разработка обязательна

**Deadline для устранения parity-gap: не более одного спринта.**

**Если реализовали фичу в Next.js:**
- ✅ Реализовать в Leptos в том же PR
- ⚠️ Или создать parity-task с deadline

**Если реализовали фичу в Leptos:**
- ✅ Убедиться, что есть в Next.js
- ⚠️ Или создать parity-task с deadline

**Трекинг паритета:**
- ✅ Issue с тегом `parity-gap`
- ✅ Обновить [PROGRESS_SUMMARY.md](./PROGRESS_SUMMARY.md)
- ✅ Контроль на code review

---

## 📊 Примеры задач с полным циклом

### Пример 1: Реализация Auth (библиотека работает ✅)

**Задача:** Фаза 1.1 — Auth Flow (Sign In / Sign Up)

**1. Анализ:**
- ✅ Библиотека `leptos-auth` существует и работает
- ✅ Backend API `/api/auth/*` готов
- ✅ Код идет в `apps/*` (UI композиция)

**2. Next.js реализация:**
```tsx
// apps/next-admin/src/lib/auth/rustok-auth.ts
export function useAuth() {
    const signIn = async (email: string, password: string) => {
        const res = await fetch('/api/auth/login', {
            method: 'POST',
            body: JSON.stringify({ email, password })
        })
        // ... обработка
    }
    return { signIn, signOut, user }
}

// apps/next-admin/src/app/auth/sign-in/page.tsx
export default function SignInPage() {
    const { signIn } = useAuth()
    return <SignInForm onSubmit={signIn} />
}
```

**3. Leptos реализация:**
```rust
// apps/admin/src/pages/auth/sign_in.rs
use leptos::*;
use leptos_auth::use_auth;  // ✅ Используем нашу библиотеку

#[component]
pub fn SignInPage() -> impl IntoView {
    let auth = use_auth();
    
    view! {
        <SignInForm 
            on_submit=move |email, password| {
                auth.sign_in(email, password)
            }
        />
    }
}
```

**4. Документация:**
- ✅ Обновить [PROGRESS_SUMMARY.md](./PROGRESS_SUMMARY.md): Phase 1.1 — 100%
- ✅ [admin-libraries-parity.md](./admin-libraries-parity.md): `leptos-auth` ✅ Закрыто

**Результат:** ✅ Паритет достигнут, обе админки работают

---

### Пример 2: Реализация Tables (библиотека в разработке ⚠️)

**Задача:** Фаза 3.1 — DataTable (Products/Users listing)

**1. Анализ:**
- ⚠️ Библиотека `leptos-table` в разработке, может иметь баги
- ✅ Backend API `/api/graphql` готов
- ✅ Код идет в `apps/*` (UI композиция) + возможно `crates/` (если shared)

**2. Next.js реализация (работает из коробки):**
```tsx
// apps/next-admin/src/app/products/page.tsx
import { DataTable } from '@/components/data-table'
import { useGraphQL } from '@/lib/graphql/client'

export default function ProductsPage() {
    const { data } = useGraphQL('query { products { id, name, price } }')
    
    return (
        <DataTable
            data={data.products}
            columns={productColumns}
        />
    )
}
```

**3. Leptos реализация (библиотека не работает):**
```rust
// Попытка 1: использовать leptos-table
use leptos_table::DataTable;  // ❌ Не компилируется

// Решение: создать issue + temporary workaround

// apps/admin/src/components/temp/simple_table.rs
// TODO: заменить на leptos-table после фикса issue #456
#[component]
pub fn SimpleTable<T>(
    data: Vec<T>,
    columns: Vec<Column>
) -> impl IntoView {
    // Простая реализация без leptos-table
    view! {
        <table class="...">
            // ... минимальная реализация ...
        </table>
    }
}

// apps/admin/src/pages/products.rs
use crate::components::temp::simple_table::SimpleTable;

#[component]
pub fn ProductsPage() -> impl IntoView {
    let products = use_query("query { products { ... } }");
    
    view! {
        <SimpleTable data=products.data() />
    }
}
```

**4. Issue:**
```markdown
Title: leptos-table не компилируется с leptos 0.6

**Library:** crates/leptos-table v0.2.0
**Problem:** Trait bound не выполнен для Column<T>
**Logs:** ...
**Workaround:** SimpleTable в apps/admin/src/components/temp/
**Deadline:** Sprint 2
```

**5. Документация:**
- ✅ [PROGRESS_SUMMARY.md](./PROGRESS_SUMMARY.md): Phase 3.1 — 50% (Next ✅, Leptos ⚠️ workaround)
- ✅ [admin-libraries-parity.md](./admin-libraries-parity.md): `leptos-table` ⚠️ В разработке
- ✅ Issue #456 с тегом `library-issue`
- ✅ Parity-gap issue #457 с тегом `parity-gap`, deadline Sprint 2

**Результат:** ⚠️ Паритет не достигнут, но работаем с workaround

---

### Пример 3: Реализация Command Palette (библиотеки нет ❌)

**Задача:** Фаза 5.1 — Command Palette (quick search)

**1. Анализ:**
- ❌ Библиотеки нет (`leptos-command-palette` не существует)
- ✅ В [rust-ui-component-catalog.md](./rust-ui-component-catalog.md) есть компонент
- ✅ Решение: адаптировать из Rust/UI

**2. Создание библиотеки:**
```bash
# 1. Создать crate
cargo new --lib crates/leptos-command-palette

# 2. Скопировать код из Rust/UI
# https://www.rust-ui.com/docs/components/command

# 3. Адаптировать
# - Убрать зависимости от Rust/UI инфраструктуры
# - Упростить API
# - Добавить документацию
```

```rust
// crates/leptos-command-palette/src/lib.rs
use leptos::*;

#[component]
pub fn CommandPalette(
    open: ReadSignal<bool>,
    on_close: impl Fn() + 'static,
    on_select: impl Fn(Command) + 'static
) -> impl IntoView {
    // ... реализация из Rust/UI, адаптированная под наши нужды ...
}
```

**3. Next.js реализация:**
```tsx
// apps/next-admin/src/components/command-palette.tsx
import { Command } from 'cmdk'  // Стандартная библиотека

export function CommandPalette({ open, onClose }: Props) {
    return (
        <Command.Dialog open={open} onOpenChange={onClose}>
            {/* ... */}
        </Command.Dialog>
    )
}
```

**4. Leptos реализация:**
```rust
// apps/admin/src/components/command_palette.rs
use leptos::*;
use leptos_command_palette::CommandPalette;  // ✅ Наша новая библиотека

#[component]
pub fn AppCommandPalette() -> impl IntoView {
    let (open, set_open) = create_signal(false);
    
    view! {
        <CommandPalette
            open=open
            on_close=move || set_open(false)
            on_select=move |cmd| handle_command(cmd)
        />
    }
}
```

**5. Документация:**
```markdown
# crates/leptos-command-palette/README.md

# Leptos Command Palette

Command palette (quick search) для Leptos приложений.

## Источник
Адаптировано из Rust/UI Command component.

## API
```rust
#[component]
pub fn CommandPalette(
    open: ReadSignal<bool>,
    on_close: impl Fn() + 'static,
    on_select: impl Fn(Command) + 'static
) -> impl IntoView
```

## Пример использования
...
```

**6. Обновление документации:**
- ✅ [admin-libraries-parity.md](./admin-libraries-parity.md):
  ```markdown
  | `cmdk` | Command palette | ✅ Закрыто | `leptos-command-palette` |
  ```
- ✅ [rust-ui-component-catalog.md](./rust-ui-component-catalog.md):
  ```markdown
  | Command | adopt | crates/leptos-command-palette | team-ui | SSR-compatible |
  ```
- ✅ [PROGRESS_SUMMARY.md](./PROGRESS_SUMMARY.md): Phase 5.1 — 100%
- ✅ Issue #458 с тегом `new-library` (closed)

**Результат:** ✅ Паритет достигнут, обе админки работают

---

## 📁 Структура crates для библиотек

### Стандартная структура crate:

```
crates/leptos-<feature>/
├── Cargo.toml
├── README.md
├── src/
│   ├── lib.rs              # Главный модуль, pub use
│   ├── components/         # UI компоненты (если есть)
│   │   ├── mod.rs
│   │   └── <component>.rs
│   ├── hooks/              # Хуки (если есть)
│   │   ├── mod.rs
│   │   └── use_<hook>.rs
│   └── utils/              # Утилиты (если есть)
│       ├── mod.rs
│       └── <util>.rs
└── examples/               # Примеры использования
    └── basic.rs
```

### Cargo.toml шаблон:

```toml
[package]
name = "leptos-<feature>"
version = "0.1.0"
edition = "2021"
authors = ["RusToK Team"]
description = "<Feature> для Leptos приложений"
license = "MIT OR Apache-2.0"

[dependencies]
leptos = { workspace = true }
# ... другие зависимости ...

[dev-dependencies]
# ... dev зависимости для тестов и примеров ...
```

### README.md шаблон:

```markdown
# leptos-<feature>

<Краткое описание>

## Установка

```toml
[dependencies]
leptos-<feature> = { path = "../../crates/leptos-<feature>" }
```

## Использование

```rust
use leptos::*;
use leptos_<feature>::*;

#[component]
fn App() -> impl IntoView {
    // ... пример ...
}
```

## API

### Components

#### `<ComponentName>`
<Описание>

**Props:**
- `prop1: Type` — описание
- `prop2: Type` — описание

### Hooks

#### `use_<hook>()`
<Описание>

**Returns:** `Type` — описание

## Источник

Адаптировано из [источник] / Создано с нуля.

## Совместимость

- ✅ CSR (Client-Side Rendering)
- ✅ SSR (Server-Side Rendering)
- Leptos: 0.6+

## License

MIT OR Apache-2.0
```

---

## 🔗 Связанные документы

**Обязательные:**
- [CRITICAL_WARNINGS.md](./CRITICAL_WARNINGS.md) — критические предупреждения
- [ADMIN_IMPLEMENTATION_PLAN.md](./ADMIN_IMPLEMENTATION_PLAN.md) — детальный план с фазами
- [admin-libraries-parity.md](./admin-libraries-parity.md) — паритет библиотек и gap log
- [admin-template-migration.md](./admin-template-migration.md) — как адаптировать шаблон

**Справочные:**
- [rust-ui-component-catalog.md](./rust-ui-component-catalog.md) — каталог Rust/UI компонентов
- [agent-execution-guide.md](./agent-execution-guide.md) — инструкция для агента
- [PROGRESS_SUMMARY.md](./PROGRESS_SUMMARY.md) — текущий прогресс

---

## 📋 Чек-лист для каждой задачи

### Перед началом
- [ ] Прочитал задачу в [ADMIN_IMPLEMENTATION_PLAN.md](./ADMIN_IMPLEMENTATION_PLAN.md)
- [ ] Проверил [admin-libraries-parity.md](./admin-libraries-parity.md) — есть ли библиотека?
- [ ] Определил, куда идет код: `crates/*` или `apps/*`?
- [ ] Понял, что делать если библиотека не работает (workaround, НЕ удалять)

### Во время реализации
- [ ] Реализовал в Next.js Admin (`apps/next-admin`)
- [ ] Реализовал в Leptos Admin (`apps/admin`) ИЛИ создал parity-task
- [ ] Если библиотека не работает — создал issue + workaround
- [ ] Если библиотеки нет — создал в `crates/*` или адаптировал из Rust/UI
- [ ] Код в правильном месте (`crates/*` для shared, `apps/*` для composition)

### После реализации
- [ ] Обновил [PROGRESS_SUMMARY.md](./PROGRESS_SUMMARY.md)
- [ ] Обновил [admin-libraries-parity.md](./admin-libraries-parity.md) если добавил библиотеку
- [ ] Обновил [rust-ui-component-catalog.md](./rust-ui-component-catalog.md) если использовал Rust/UI
- [ ] Создал PR с понятным commit message
- [ ] Паритет Next ↔ Leptos достигнут ИЛИ создан parity-gap issue

---

## 💡 Советы и best practices

### 1. Начинайте с Next.js
Next.js обычно быстрее для прототипирования UI/UX. Сделайте референс в Next.js, а потом переносите в Leptos.

### 2. Копируйте Tailwind классы
При переносе UI из Next.js в Leptos просто копируйте Tailwind классы — они одинаковые.

```tsx
// Next.js
<div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
```

```rust
// Leptos
<div class="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
```

### 3. Не дублируйте бизнес-логику
API запросы, валидация, state management — все это должно быть в `crates/*`, не дублируйте в `apps/*`.

### 4. Документируйте API библиотек
Каждая библиотека в `crates/*` должна иметь README с примерами использования.

### 5. Создавайте маленькие PR
Не пытайтесь реализовать всю фазу в одном PR. Разбивайте на маленькие задачи.

### 6. Используйте временные workaround'ы
Если библиотека не работает — не застревайте. Создайте workaround и продолжайте. Вернетесь после фикса.

### 7. Обновляйте документацию сразу
Не откладывайте обновление документации "на потом". Делайте это сразу после реализации.

---

**Версия:** 1.0  
**Дата:** 2026-02-13  
**Автор:** CTO Agent

**Вопросы:** Создайте issue с тегом `workflow` или `documentation`
